version: "3.9"
services:
  nl2sql:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file:
      - ../.env
    ports:
      - "8000:8000"
    command: ["uv", "run", "python", "main.py", "prod"]
